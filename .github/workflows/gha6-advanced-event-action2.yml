name: advanced event activity type action workflow  

on: 
    push:  
    issues:
        types:
        - opened
        - labeled
jobs:  
  
  test:
    runs-on: ubuntu-latest  
    
    steps:  
      - name: Run Test  
        run: |  
          echo github.event_name: "${{ github.event_name.types }}" 
          echo Run Test  
  
  